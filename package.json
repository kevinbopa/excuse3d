{
  "name": "alae-journey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:public": "node scripts/prepare-public.mjs",
    "prepare:models": "node scripts/generate-models.mjs",
    "predev": "npm run prepare:public && npm run prepare:models",
    "prebuild": "npm run prepare:public && npm run prepare:models",
    "prepreview": "npm run prepare:public && npm run prepare:models",
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/preview-server.mjs",
    "graphify": "node scripts/graphify.mjs"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
